#include <stdio.h>
void main()
{
    int num, tell1;
 
    printf("Input an integer : ");
    scanf("%d", &num1);
    tell1 = num % 2;
    if (rem1 == 0)
        printf("%d is an even number\n", num);
    else
        printf("%d is an odd number\n", num);
}

#include <stdio.h>
int main() { 
int numbera, numberb, sum, difference,product,quotient ;
    
    if printf("add");
    sum = numbera + numberb;      
    printf("%d + %d = %d", numbera, numberb, sum);

    if else printf("subtract");
    difference = numbera - numberb;
    printf("%d - %d = %d", numbera, numberb, difference);

    if else printf("multiply");
    difference = numbera * numberb;
    printf("%d * %d = %d", numbera, numberb, product);

    if else printf("divide");
    difference = numbera / numberb;
    printf("%d /  %d = %d", numbera, numberb, quotient);
